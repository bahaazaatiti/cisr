title: Library

options:
  preview: true
  changeTemplate: false

icon: folder

columns:
  - width: 1/3
    fields:
      description:
        label: Folder description
        type: textarea
        size: small
        translate: true
        help: Optional intro shown above the listing.
  - width: 2/3
    sections:
      children:
        type: pages
        headline: Contents
        templates:
          - library
          - library-item
        sortBy: title asc
        info: "{{ page.intendedTemplate }}"
