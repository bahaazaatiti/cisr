title: Videos

icon: video

options:
  preview: false
  changeTemplate: false

sections:
  list:
    extends: sections/pages-by-status
    headline: Videos
    status: all
    template: video
    sortBy: date desc
    info: "{{ page.date.toDate('Y-m-d') }} · {{ page.source_type.or('youtube') }}"
    empty: No videos yet. Use + to add one (YouTube link or magnet).
